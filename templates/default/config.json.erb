{
  "version": "1.0",
  "local_store": "<%= node['git2consul']['local_store'] %>",
  "logger" : {
    "name" : "git2consul",
    "streams" : <%= JSON.pretty_generate(node['git2consul']['log_streams']) %>
  },
  "repos" : <%= JSON.pretty_generate(node['git2consul']['repositories']) %>
}
